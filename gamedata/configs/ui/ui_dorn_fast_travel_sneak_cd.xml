<!-- Dorn's Fast Travel: sneak-travel cooldown countdown label. Parsed directly (not merged into
     an existing schema) and attached as a real child of the live PDA dialog (ActorMenu.get_pda_menu()),
     so it renders as part of the PDA's own UI - including the 3D holographic PDA projection (g_3d_pda)
     - instead of a flat always-on-top HUD overlay. x/y are relative to the PDA dialog's own top-left
     corner; nudge them here if they land outside your PDA skin's visible panel. -->
<w>
	<dft_sneak_cd_label x="140" y="55" width="420" height="30">
		<text font="letterica18" complex_mode="1" vert_align="t" align="l" r="255" g="224" b="64" a="255"/>
	</dft_sneak_cd_label>
</w>
